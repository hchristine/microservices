// Users microservice subjects
export const REGISTER_USER = {
  cmd: 'REGISTER_USER'
};

export const LOGIN_USER = {
  cmd: 'LOGIN_USER',
};

// Products microservice subjects
export const GET_PRODUCTS = {
  cmd: 'GET_PRODUCTS',
};

export const CREATE_PRODUCT = {
  cmd: 'CREATE_PRODUCT',
};

// Orders microservice subjects
export const CREATE_ORDER = {
  cmd: 'CREATE_ORDER',
}

export const GET_PRODUCT_BY_ID = {
  cmd: 'GET_PRODUCT_BY_ID',
}

export const CREATE_FEEDBACK = {
  cmd: 'CREATE_FEEDBACK'
};

export const GET_FEEDBACK = {
  cmd: 'GET_FEEDBACK'
};

export const GET_ORDER_BY_QUERY = {
  cmd: 'GET_ORDER_BY_QUERY'
};

export const CREATE_NOTIFICATION = {
  cmd: 'CREATE_NOTIFICATION'
};

export const GET_NOTIFICATIONS = {
  cmd: 'GET_NOTIFICATIONS'
}

export const CHARGE_PAYMENT = {
  cmd: 'CHARGE_PAYMENT',
}

export const USER_CREATED = {
  cmd: 'USER_CREATED',
}

export const ORDER_CREATED = {
  cmd: 'ORDER_CREATED',
}